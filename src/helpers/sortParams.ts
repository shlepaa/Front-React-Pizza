import { TypeParams } from '../interfaces/TypeParams';

const sortParams: TypeParams[] = [
	{ param: 'rating', title: 'популярности' },
	{ param: 'currentPrice', title: 'цене' },
	{ param: 'title', title: 'алфавиту' },
];

export default sortParams;
